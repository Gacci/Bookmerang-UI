import { InfiniteScrollView } from './infinite-scroll-view';

describe('InfiniteScrollView', () => {
  it('should create an instance', () => {
    expect(new InfiniteScrollView()).toBeTruthy();
  });
});
